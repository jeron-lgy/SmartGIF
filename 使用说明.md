# 动图压制转换器

双击 `启动动图转换器.cmd` 或 `启动网页动图转换器.cmd` 启动。工具会开启本地服务并在浏览器中展示基于 `Vue 3 + Naive UI` 的操作界面。

工具需要本机可用的 `Python` 与 `FFmpeg`，当前电脑已检测可用。前端成品已构建完成，日常使用不需要运行 `npm`。

## 网页界面

网页工作台提供以下能力：

| 区域 | 功能 |
| --- | --- |
| 输入视频 | 直接选择当前目录中的视频，或将新视频导入本地工作区。 |
| 输出格式 | 多选 `GIF`、`WebP`、`APNG`、`AVIF`，一次任务分别寻找最佳结果。 |
| 压缩设置 | 一键切换三档预设，填写如 `10 MB` 的严格体积上限。 |
| 高级参数 | 调节宽度、帧率、色数、WebP 质量、AVIF CRF、编码速度。 |
| 处理日志 | 实时显示自动试压过程，可在处理中取消任务。 |
| 效果对比 | 转换完成后，同屏循环播放各格式并支持直接下载。 |

输出结果保存在 `outputs` 目录下按任务区分的子文件夹中；上传的视频保存在 `uploads` 目录中。

## 支持格式

| 格式 | 适用场景 | 特点 |
| --- | --- | --- |
| WebP | 网页、聊天、一般发布 | 兼容性和压缩比均衡，通常优先选择 |
| AVIF | 支持现代浏览器/应用的场景 | 压缩率最高，编码比 WebP 慢 |
| GIF | 平台兼容兜底 | 兼容最广，但颜色、帧率与清晰度损失最大 |
| APNG | 透明背景或需要 PNG 色彩特征 | 质量好但视频类素材体积效率偏低 |

## 压缩率预设

| 预设 | 最大尺寸/帧率策略 | 适合用途 |
| --- | --- | --- |
| 低压缩（质量优先） | 默认保留源尺寸和源帧率 | 限制大小时尽可能保住质量，耗时较长 |
| 中压缩（均衡） | 最大 `1200 px / 20 fps` | 日常发布，速度与效果均衡 |
| 高压缩（体积优先） | 最大 `720 px / 12 fps` | 需要快速得到更小文件 |

修改参数后即采用自定义设置；重新选择预设会覆盖参数框内的值。

## 影响画质与文件大小的选项

| 选项 | 影响 |
| --- | --- |
| 最大宽度 | 最直接影响清晰度和体积。`0` 表示保留源宽度；尺寸越大文件通常增长越明显。 |
| 最大帧率 | 影响动作流畅度。`0` 表示保留源帧率；运动画面中帧率越高体积越大。 |
| GIF/APNG 色数 | `16-256`。色数少会出现色带，但可减少体积。仅影响 GIF/APNG。 |
| WebP 质量 | `0-100`，数值越高质量越好、文件越大。仅影响 WebP。 |
| AVIF CRF | `0-63`，数值越低质量越好、文件越大。仅影响 AVIF。 |
| 编码速度 | `0-8`，越低通常能用更长时间换取更好的压缩效率；AVIF 上差异最明显。 |

## 限制文件大小

在“最大文件大小”填写 `10` 并保持“自动寻找满足上限的最佳参数”勾选，输出会严格小于或等于 `10,000,000` 字节。

自动限容会执行多轮试压：

1. 从当前预设或自定义参数开始。
2. 对 WebP/AVIF 优先微调编码质量，并守住合理质量下限。
3. 仍然超限时，自动降低尺寸或帧率。
4. 对 GIF/APNG 还会调整调色板色数。
5. 在已经满足限制的候选中，保留清晰度、流畅度和编码质量综合评分最高的一份。

AVIF 在开启自动限容且 FFmpeg 支持时会使用更快的 SVT-AV1 编码，否则自动回退到兼容编码；自动寻优范围控制在最大 `1600 px / 20 fps`，`20 MB` 以内的任务从 `1200 px / 15 fps` 开始试压。这可以避免 4K 视频首轮等待过久或占用过高内存。需要导出更高分辨率的 AVIF 时，可关闭自动试压并手动指定参数，但处理时间会明显增加。

如同时勾选多个格式，每种格式会分别优化并输出对应结果。勾选“完成后生成网页对比预览”后，可以在同一网页里观看比较。

## 命令行调用

转换核心仍支持直接用命令行运行，例如输出 `10 MB` 以内的 WebP 与 AVIF：

```powershell
python .\animation_converter.py --input ".\输入视频.mp4" --formats webp,avif --target-mb 10 --preset low
```

常用参数：

```text
--formats gif,webp,apng,avif
--preset low|medium|high
--target-mb 10
--width 1200
--fps 20
--colors 192
--webp-quality 80
--avif-crf 24
--speed 6
--no-preview
```

需要手动启动网页服务时，可使用：

```powershell
python .\animation_server.py
```

如需修改网页前端源码，文件位于 `webui\src`；重新构建使用：

```powershell
cd .\webui
npm.cmd install
npm.cmd run build
```
